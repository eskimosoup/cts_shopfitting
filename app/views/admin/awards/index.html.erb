<% title Award.model_name.human.pluralize %>

<div class="admin_functions">
  <%= link_to "New #{ Award.model_name.human }", new_admin_award_path %>
</div>

<%= informant_table @awards, {
      :headings => [ '', 'Name', 'Image', 'Display', 'Display in footer', '', '' ], 
      :body => [ it_sort, :name, it_edit_images(:image), it_ajax_toggle(:display), it_ajax_toggle(:display_footer), it_edit, it_destroy ]
    } %>
