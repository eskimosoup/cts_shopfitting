<% title HomePage.model_name.human.pluralize %>

<div class="admin_functions">
  <%= link_to "New #{ HomePage.model_name.human }", new_admin_home_page_path %>
</div>

<%= informant_table @home_pages, {
      :headings => [ 'Name', 'Image', 'Display', '', '' ], 
      :body => [ :name, it_edit_images(:image), it_ajax_toggle(:display), it_edit, it_destroy ]
    } %>
